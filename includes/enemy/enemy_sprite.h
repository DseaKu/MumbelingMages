#ifndef ENEMY_SPRITE_H
#define ENEMY_SPRITE_H
typedef enum {
  INACTIVE = 0,
  WALKING,
  IDLE,
  TAKE_DEMAGE,
  DYING,
  SPAWNING,
  DEAD,
  PROCESSES_ACTING_FORCES,
  SIZE_STATE_ID,
} StateID;

typedef enum {
  NONE = 0,
  GOBLIN,
  SKELLETON,
  BAT,
  SIZE_SPRITE_ID,
} SpriteID;

#endif // !ENEMY_SPRITE_H
